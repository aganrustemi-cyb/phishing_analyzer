# phishing_analyzer
A Python CLI tool that takes a suspicious .eml file, automatically checks all IPs, URLs, and attachments against VirusTotal and AbuseIPDB, maps findings to MITRE ATT&amp;CK techniques, and uses GPT-4o to generate a complete SOC incident report — the same report a Tier 1 analyst would have to write manually after triaging a phishing alert.
